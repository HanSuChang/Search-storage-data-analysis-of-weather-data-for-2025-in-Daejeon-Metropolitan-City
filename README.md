#Daejeon 2025 Weather Data Analysis

# 기능 설명

- 대전 2025 일별 기상 데이터 분석
  - 기상청 ASOS 일자료(CSV)를 불러와 대전 지역의 2025년 일별 기상 데이터를 분석합니다.
  - 날짜, 기온, 강수량, 습도, 풍속 등 다양한 기상 요소를 다룹니다.

- CSV 데이터 로드 및 전처리
  - CSV 파일을 선택하여 불러온 후 자료형 변환 및 정렬을 수행합니다.
  - 날짜(`일시`) 컬럼을 datetime 타입으로 변환하고 숫자형 컬럼을 자동 처리합니다.
  - 전처리된 데이터는 기준 데이터와 현재 데이터로 분리 관리됩니다.

- 조건 판단 및 필터링 기능
  - 특정 컬럼에 대해 `>`, `>=`, `<`, `<=`, `==`, `!=`, `contains`, `in` 조건을 적용할 수 있습니다.
  - 조건을 만족하면 0, 불만족하면 1로 표시되는 판단 열을 자동 생성합니다.
  - 조건 만족 데이터만 추출하여 상세 분석이 가능합니다.

- 상세 데이터 조회 및 시각화
  - 조건을 만족하는 데이터에 대해 표(Table)와 함께 선 그래프(Line Chart)를 표시합니다.
  - 날짜 기준 시계열 데이터 변화를 직관적으로 확인할 수 있습니다.

- 계절별 상관관계 히트맵 분석
  - 봄/여름/가을/겨울로 계절을 구분하여 변수 간 상관관계를 히트맵으로 시각화합니다.
  - 상관계수 절댓값 기준으로 주요 변수(K개)를 자동 선택합니다.
  - 데이터가 부족한 계절은 자동으로 제외 처리합니다.

- 통계 요약 및 월별 평균 분석
  - 전체 데이터에 대한 요약 통계량(count, mean, std 등)을 제공합니다.
  - 1월~12월 월별 주요 기상 요소 평균을 계산하여 표 형태로 출력합니다.

- 이상치 제거 기능
  - 최대 풍속(m/s)을 기준으로 IQR 방식의 이상치 제거를 지원합니다.
  - 물리적 한계를 고려하여 음수 풍속은 자동 보정합니다.

- 검색 결과 저장 및 불러오기
  - 특정 날짜의 검색 결과를 CSV 파일로 저장할 수 있습니다.
  - 저장된 CSV 파일을 목록 형태로 불러와 미리보기가 가능합니다.

- 달력 기반 기상 정보 시각화
  - 달력 UI를 통해 월별 기상 데이터를 한눈에 확인할 수 있습니다.
  - 날씨 상태에 따라 이모지(☀️, 🌧️, 🌫️, ❄️)로 표현됩니다.
  - 날짜 클릭 시 해당 날짜의 상세 기상 정보를 확인할 수 있습니다.

- Tkinter 기반 GUI
  - 모든 기능은 터미널이 아닌 GUI 환경에서 조작 가능합니다.
  - 표(Treeview), 팝업, 메뉴바, 그래프 영역을 분리하여 구성했습니다.
 






# 사용된 파이썬 기술

- pandas
  - CSV 데이터 로드 및 데이터프레임 기반 전처리 수행
  - 날짜 파싱, 결측치 처리, 그룹화(groupby), 통계 분석에 활용

- NumPy
  - 조건 판단 결과 생성 및 수치 연산 처리
  - 히트맵 계산 및 상관계수 처리에 사용

- Tkinter (GUI)
  - `Tk`, `Toplevel`, `Frame`, `Button`, `Label`, `Treeview` 등을 활용한 GUI 구성
  - 메뉴바, 팝업창, 달력 UI, 데이터 테이블 구현
  - 이벤트 바인딩을 통한 사용자 입력 처리

- Matplotlib
  - 선 그래프(Line Chart) 및 계절별 상관 히트맵 시각화
  - Tkinter와 연동(`FigureCanvasTkAgg`)하여 GUI 내부에 그래프 표시

- datetime / calendar
  - 날짜 변환 및 월별/계절별 분류 처리
  - 달력 UI 구성 및 날짜 계산에 사용

- 함수 및 클래스 기반 구조
  - `WeatherApp`, `WeatherGUI` 클래스를 중심으로 기능을 모듈화
  - 데이터 로드, 필터링, 시각화, 저장 기능을 함수 단위로 분리하여 관리

- 문자열 처리 및 입력 파싱
  - 다양한 날짜 입력 형식(YYYYMMDD, YYMMDD 등)을 처리하는 파서 구현
  - 컬럼명 정리 및 출력용 텍스트 가공 처리

- 예외 처리 (try-except)
  - 파일 로드 오류, 형 변환 실패, 사용자 입력 오류에 대비한 안정적인 예외 처리
  - GUI 실행 중 오류 발생 시 프로그램이 중단되지 않도록 설계
 


## 역할 분담 및 기여도

본 프로젝트는 팀 프로젝트로 진행되었으며, 본인은 전체 시스템의 핵심 기능과 UI를 중심으로 구현을 담당하였습니다.

# 본인(한수창) 담당 내용
- **전체 GUI(UI) 설계 및 구현**
  - Tkinter 기반의 전체 사용자 인터페이스를 직접 설계 및 구현
  - 화면 구성, 버튼 배치, 테이블(Treeview), 팝업 창 등 전반적인 UI 구조 담당

- 달력 기반 기상 정보 UI 구현
  - 월별 달력 UI를 직접 구현
  - 날짜 클릭 시 해당 날짜의 기상 정보를 표시하도록 연동
  - 날씨 상태를 이모지로 시각화하는 기능 구현

- 기상 데이터 검색 기능 구현
  - 날짜 입력 및 조건 기반 기상 데이터 검색 기능 구현
  - 다양한 날짜 형식을 처리하는 입력 파서 직접 작성

- 검색 결과 저장 및 불러오기 기능 구현
  - 검색된 기상 데이터를 CSV 파일로 저장하는 기능 구현
  - 저장된 파일을 다시 불러와 확인할 수 있는 기능 구현

- 계절별 상관관계 히트맵 시각화 구현
  - 기상 변수 간 상관관계를 계산하고 히트맵으로 시각화
  - 계절 구분 로직 및 주요 변수 자동 선택 기능 구현

- 기상청 ASOS(OBS) 데이터 수집
  - 기상청 ASOS 일자료(OBS) 데이터를 직접 수집 및 정리
  - 프로젝트에 적합한 형태로 전처리하여 분석에 활용

# 팀원 담당 내용
- 기상 데이터 분석 보조 및 일부 통계 계산
- 결과 해석 및 문서 정리
- 프로젝트 발표 자료(PPT) 보조 작성






